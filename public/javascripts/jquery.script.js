(function($){
	console.log("jquery insert here.");
	/*$(document).ready(function(){
		$('body').loadingbar({
            done: function(data) {
              $.each( data.shots, function( i, item ) {
                $( "<img/>" ).attr( "src", item.image_teaser_url ).prependTo( $("#frame") );
                if ( i === 11 ) {
                  return false;
                }
              });
            }
          });
	})*/
}(jQuery));